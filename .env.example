# Environment Configuration Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ============================================
# REQUIRED - Application will not start without these
# ============================================

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_secure_database_password
DB_NAME=nestjs_starter

# Authentication Secrets (Generate with: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_min_32_characters_long
EMAIL_VERIFICATION_SECRET=your_email_verification_secret_min_32_characters_long
PASSWORD_RESET_SECRET=your_password_reset_secret_min_32_characters_long

# Email Service (SMTP)
MAILER_HOST=smtp.gmail.com
MAILER_PORT=587
MAILER_USER=your_email@gmail.com
MAILER_PASS=your_app_password_or_api_key

# ============================================
# OPTIONAL - Application will work without these
# ============================================

# Application
NODE_ENV=development
PORT=3000

# Rate Limiting
RATE_LIMIT=300
RATE_LIMIT_TTL=60000

# OAuth Providers (Optional)
# Google OAuth - Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Facebook OAuth - Get from: https://developers.facebook.com/apps/
FACEBOOK_CLIENT_ID=your_facebook_app_id
FACEBOOK_CLIENT_SECRET=your_facebook_app_secret
FACEBOOK_CALLBACK_URL=http://localhost:3000/auth/facebook/callback

# File Storage - S3-compatible (Optional)
# AWS S3
STORAGE_ACCESS_KEY_ID=your_aws_access_key_id
STORAGE_SECRET_ACCESS_KEY=your_aws_secret_access_key
STORAGE_BUCKET=your-bucket-name
STORAGE_REGION=us-east-1
STORAGE_ENDPOINT=https://s3.amazonaws.com

# DigitalOcean Spaces example:
# STORAGE_ENDPOINT=https://fra1.digitaloceanspaces.com
# STORAGE_REGION=fra1

# MinIO example (local development):
# STORAGE_ENDPOINT=http://localhost:9000
# STORAGE_REGION=us-east-1

# Stripe Payment Integration (Optional)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_API_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret

# ============================================
# DEVELOPMENT ONLY
# ============================================

# Database Logging (set to true for SQL query debugging)
# DB_LOGGING=true

# Database SSL (set to true for production)
# DB_SSL=true

# ============================================
# PRODUCTION RECOMMENDATIONS
# ============================================
# 1. Use environment variables or a secrets manager (AWS Secrets Manager, HashiCorp Vault)
# 2. Never commit .env to version control
# 3. Use strong, randomly generated secrets (min 32 characters)
# 4. Enable database SSL in production
# 5. Use shorter JWT expiration times (15m-30m) with refresh tokens
# 6. Enable rate limiting with lower limits
# 7. Use HTTPS for all URLs (callback URLs, frontend URL, etc.)
# 8. Regularly rotate secrets and API keys
